@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/access-denied");
        // If prod still acts flaky, use a hard reload:
        // Nav.NavigateTo("/access-denied", forceLoad: true);
    }
}